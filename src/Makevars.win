CXX_STD = CXX17
#PKG_CXXFLAGS = -Wall -pedantic #non-portable

R_SCRIPT = $(R_HOME)/bin$(R_ARCH)/Rscript.exe

PKG_CPPFLAGS = $(shell $(R_SCRIPT) --vanilla -e "Rcpp:::CxxFlags()")
PKG_LIBS     = $(shell $(R_SCRIPT) --vanilla -e "Rcpp:::LdFlags()")
